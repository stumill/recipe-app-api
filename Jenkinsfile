pipeline {
  agent any
  stages {
    stage('Build') {
      steps {
        echo 'Building application on ${BRANCH_NAME}...'
      }
    }
    stage('Test') {
      steps {
        echo 'Testing application on ${BRANCH_NAME}...'
      }
    }
  }
}
